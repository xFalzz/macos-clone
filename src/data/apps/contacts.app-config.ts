import { createAppConfig } from '__/helpers/create-app-config';

export const contactsAppConfig = createAppConfig({
  showInDock: false,
  title: 'Contacts',
  resizable: true,
  height: 500,
  width: 700,
});
